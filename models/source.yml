version: 2

sources:
  - name: flight_data  # This is your dataset ID
    database: data-management2-458610  # Your project ID
    tables:
      - name: live_flight_data  # This should match the actual table name for live flight data
        description: "This table contains real-time flight data"
      - name: static_flight_data  # This should match the actual table name for static airport data
        description: "This table contains static airport data"
